package livingObjects;

import java.awt.Point;

/**
 * A class, distinct from the AWT's Point class (though it uses Point for all of
 * its implementation). Used to track locations within the game world.
 *
 * @author blad
 */
@SuppressWarnings("serial")
public class Location extends Point {
}
